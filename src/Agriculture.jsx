import React, { useEffect } from 'react'

const Agriculture = () => {
  useEffect(() => {
      window.scrollTo(0, 0);
    }, []);
  return (
    <div>Agriculture</div>
  )
}

export default Agriculture